import React from 'react'
import PropTypes from 'prop-types'

const PostImages = ({ images }) => {
    return (
        <div>
            working...
        </div>
    )
}

PostImages.prpoTypes = {
    images: PropTypes.arrayOf(PropTypes.object)
}

export default PostImages
